
import React from 'react';
import PageItem from '../PageItem';
import promptEngineeringContent from './promptEngineeringContent';

export default function PromptEnginnering() {
    return (
        PageItem(promptEngineeringContent)
    );
}
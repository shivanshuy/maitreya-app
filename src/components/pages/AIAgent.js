
import React from 'react';
import PageItem from '../PageItem';
import aiAgentContent from './aiAgentContent';

export default function Datasets() {
    return (
        PageItem(aiAgentContent)
    );
}
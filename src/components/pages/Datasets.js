
import React from 'react';
import PageItem from '../PageItem';
import datasetsContent from './datasetsContent';

export default function Datasets() {
    return (
        PageItem(datasetsContent)
    );
}

import React from 'react';
import PageItem from '../PageItem';
import elasticContent from './elasticContent';

export default function WebServer() {
    return (
        PageItem(elasticContent)
    );
}

import React from 'react';
import PageItem from '../PageItem';
import llamaCppContent from './llamaCppContent';

export default function LlamaCpp() {
    return (
        PageItem(llamaCppContent)
    );
}
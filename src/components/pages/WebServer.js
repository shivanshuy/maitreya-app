
import React from 'react';
import PageItem from '../PageItem';
import webServerContent from './webServerContent';

export default function WebServer() {
    return (
        PageItem(webServerContent)
    );
}
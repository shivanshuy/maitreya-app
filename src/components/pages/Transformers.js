
import React from 'react';
import PageItem from '../PageItem';
import transformersContent from './transformersContent';

export default function Transformers() {
    return (
        PageItem(transformersContent)
    );
}